version: '3'
services:
  postgres:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1363
      POSTGRES_DB: invoicetracker
    ports:
      - "5432:5432"
